package com.example.sakuku.ui.screens.profil

import androidx.compose.runtime.Composable

// TODO(user): placeholder sementara biar compile - isi beneran nyusul, ikutin pola
// KtpDataDiriScreen.kt (field Email + Nomor HP, viewModel = hiltViewModel(), dst).
@Composable
fun KontakScreen(
    onBack: () -> Unit = {}
) {
}